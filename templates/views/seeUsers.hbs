  <div id="myCarousel" class="carousel slide" data-bs-ride="carousel"></div>
  
  <div class="container marketing text-center" style="height: 80px;">
    
      
</div>
<div class="container text-center">
 <h1>Users!!</h1>
</div>

 <div class="container marketing text-center" >
      <div class="row">
          <table class="table table-sm" >
              <thead>
                  <th scope="col">First Name</th>
                  <th scope="col">Last Name</th>
                  <th scope="col">Email</th>
                  <th scope="col">Delete User</th>
              </thead>
              <tbody>   
                {{#each data}}
                 <tr>
                    <td>{{this.fname}}</td>
                    <td>{{this.lname}}</td>
                    <td>{{this.email}}</td>
                    {{!-- <td   ><button type="button" class="btn-close deleteuser"   id="{{this._id}}" aria-label="Close"></button></td> --}}
                    <td><button class=" deleteuser btn-close"  id="{{this._id}}"></button></td>
                 </tr>
                 
                {{/each}}
               <p id="demo"></p>

              </tbody>
          </table>
          <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
          <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
          <script>
              $(document).on('click','.deleteuser',function(e){
              const id =e.target.id
         
                    var txt;
                    if (confirm("Are you sure you want to delete this user??")) {
                        txt = true;
                    } else {
                        txt = false;
                    }
                    
                    if(txt){
                            axios({
                            method: 'get',
                            url: '/delete',
                            params: {
                                id:id
                            }
                            }).then((response)=>{
                              
                               if(response){
                                 window.location.reload();
                               }
                            })
                    }
                
              
            })
          </script>
      </div>


  </div>